# Independent deployment boundary

## Current public surface

- Repository: `https://github.com/lghui12138/h2-testlens-hackathon`
- Pages URL: `https://lghui12138.github.io/h2-testlens-hackathon/` (GitHub Pages project URL; custom-domain routing is not used by the submission path)
- Rendered public URL: `https://lghui12138.github.io/projects/h2-testlens.html` (GitHub Pages API currently reports this public custom-domain route)
- GitHub repository: `https://github.com/lghui12138/h2-testlens-hackathon`
- Workflow: `.github/workflows/deploy-pages.yml`
- Build artifact: `_site/` generated by `scripts/prepare-github-pages.mjs`

The Pages build is intentionally static. It runs the audited browser analyzer, loads sample/config assets by relative paths, keeps raw CSV rows in the browser, and falls back to the local evidence draft when `/api/ai-draft` is unavailable.

## HTTPS status

As of 2026-08-24, GitHub Pages reports `public: true` and `https_enforced: false`; enabling HTTPS was rejected by the GitHub API with `404: The certificate does not exist yet`. Do not claim that the custom domain has enforced HTTPS until GitHub provisions the certificate. This is an external domain/certificate state, not an application-code failure; the GitHub repository and project Pages URL remain the stable source links.

## Authentication boundary

The public Pages surface currently has no account system. Local history is browser-local only; it is not a user account or shared database. Do not add a fake `localStorage` registration flow.

Normal registration requires an approved external identity provider such as email/password or GitHub OAuth, plus server-side/session and data-access rules. Provider secrets must never be committed to this public repository or exposed as frontend secrets. When an auth provider is selected, add the provider callback/allowed-origin settings and test sign-up, email confirmation, sign-in, sign-out, password reset, and unauthorized data access before calling the registration flow production-ready.

## Release gate

Every push to `main` runs `npm run check:submission`, builds `_site/`, uploads a GitHub Pages artifact, and deploys it. A green Actions run proves build/deployment integrity; it does not prove enterprise standards approval or user-authentication readiness.

The public `config/release-summary.json` has two truthful modes. A local checkout keeps the checked-in `unbound-local-fallback`. During a GitHub Actions static deployment, when no package receipt is available, `scripts/prepare-github-pages.mjs` first runs `npm run package:submission`; a package smoke, ZIP integrity or SHA-256 failure aborts Pages preparation. A successful cloud receipt therefore records `GITHUB_SHA`/`GITHUB_RUN_ID` and a verified artifact. The `not_run` fallback is reserved for local/non-Actions previews and does not represent a successful cloud release.
